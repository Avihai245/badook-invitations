import { AccountSkeleton } from '@/features/invitations/app/skeletons';

/** Shaped like the account screen (§9B.3-H: skeletons, not spinners). */
export default function AccountLoading() {
  return <AccountSkeleton />;
}
