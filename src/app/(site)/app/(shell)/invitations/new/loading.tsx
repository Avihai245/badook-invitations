import { GallerySkeleton } from '@/features/invitations/app/skeletons';

/** Shaped like the design gallery: head, event-type chips, posters. */
export default function GalleryLoading() {
  return <GallerySkeleton />;
}
