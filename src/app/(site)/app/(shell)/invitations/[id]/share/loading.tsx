import { ShareSkeleton } from '@/features/invitations/app/skeletons';

/** Shaped like the share screen, under the invitation's header (§9B.3-H: skeletons, not spinners). */
export default function ShareLoading() {
  return <ShareSkeleton />;
}
