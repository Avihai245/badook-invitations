import { OverviewSkeleton } from '@/features/invitations/app/skeletons';

/** Shaped like the overview, under the invitation's header (which stays). */
export default function OverviewLoading() {
  return <OverviewSkeleton />;
}
