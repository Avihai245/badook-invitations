import { EditorSkeleton } from '@/features/invitations/app/skeletons';

/** Shaped like the editor: top bar, rail and form, the canvas with the phone (§9B.3-H). */
export default function EditorLoading() {
  return <EditorSkeleton />;
}
