# KICKOFF — paste this into Claude Code (repo root, after unzipping the kit)

Read `docs/invitations/MASTER_PROMPT.md` in full. Then open `docs/invitations/design-reference/invitation.html` (try `?lang=en` and `?open=1`) and `docs/invitations/design-reference/app.html`, and look at every image in `docs/invitations/design-reference/screenshots/`. Skim `invitation-templates-pack/` (8 templates) and `docs/invitations/fixtures/`.

Then do **Step 0 only** (§1 of the master prompt): inspect this repo and report the stack mapping, tenancy model, existing design system, i18n/RTL status, asset/storage setup and your proposed file structure for the feature. Do not write feature code yet.

After I approve, build phase by phase — P0 → P1 → P2 → P3 → P4 — exactly as defined in §11, running the Design QA gate and the relevant acceptance tests (§12) at the end of each phase, and stop for my approval after every phase with the report described in §13.
