# Invitation templates pack v2

8 original templates. Each folder: `manifest.json` (TemplateManifest v2), `defaults.json` (TemplateDefaults — HE+EN seed copy per event type), `ASSETS.md` (checklist of media files to produce).

Install: copy each folder to `public/templates/<id>/` (JSON files may instead live in `src/templates/<id>/` — follow the build prompt). Media files are produced with `invitation-templates-kit-he.md`; until they exist, the app renders placeholders.

| id | categories |
|---|---|
| `atara` | bar_mitzvah, bat_mitzvah |
| `caesarea-shore` | wedding, engagement, save_the_date |
| `honey-meadow` | baby_shower, birthday, brit |
| `nitzan` | brit, baby_shower |
| `papercut-gold` | wedding, engagement |
| `ramon-dusk` | wedding, engagement, henna |
| `rooftop-dusk` | birthday, engagement, corporate |
| `sahar-bordeaux` | wedding, engagement, save_the_date |
